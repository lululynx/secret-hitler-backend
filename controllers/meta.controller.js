'use strict';

module.exports.newGame = (socket) => {
  console.log(socket);
}

module.exports.joinGame = (socket) => {
  console.log(socket);
}

module.exports.leaveGame = (socket) => {
  console.log(socket);
}