'use strict';

module.exports.voteOnChancellor = (socket) => {
  console.log(socket);
}

module.exports.suggestChancellor = (socket) => {
  console.log(socket);
}

module.exports.pickPolicies = (socket) => {
  console.log(socket);
}

module.exports.executePlayer = (socket) => {
  console.log(socket);
}

module.exports.vetoPolicy = (socket) => {
  console.log(socket);
}